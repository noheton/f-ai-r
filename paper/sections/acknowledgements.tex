\section*{Acknowledgements}
\label{sec:acks}
\addcontentsline{toc}{section}{Acknowledgements}

This paper was written with substantial assistance from a Large
Language Model (Anthropic Claude, model identifier
\texttt{claude-opus-4-7}) operating under the per-role prompts
committed to \texttt{agents/} and the harness rules in
\texttt{CLAUDE.md}. The model is acknowledged here per the seventh
integrated practice (\S\ref{sec:practice-legal}); it is not an author.
The provenance graph in \texttt{doc/provenance.ttl} records, for each
section of this manuscript, the activity that produced it and the
agents (human and AI) that participated in that activity.

The pattern here distilled was developed on the source repository
\texttt{noheton/Obscurity-Is-Dead} by the same author; the present
paper is the abstraction that strips the source's domain content and
publishes the methodological layer. We thank the maintainers of the
underlying open-source toolchain --- TeX Live, \texttt{latexmk},
\texttt{biber}, \texttt{rdflib}, the Python ecosystem, and GitHub
Actions --- without which the pattern would be more expensive than it
is.
